<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>org.alice</groupId>
	<artifactId>alice-parent</artifactId>
	<packaging>pom</packaging>
	<version>0.0.1-SNAPSHOT</version>

	<name>Alice</name>
	<url>http://www.alice.org/</url>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
	</properties>

	<build>
		<plugins>
			<plugin>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<source>1.6</source>
					<target>1.6</target>
					<encoding>UTF-8</encoding>
					<!-- Only include Java source files in the compilation. -->
					<includes>
						<include>**/*.java</include>
					</includes>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>versions-maven-plugin</artifactId>
				<version>2.1</version>
			</plugin>
			<plugin>
				<groupId>org.zanata</groupId>
				<artifactId>zanata-maven-plugin</artifactId>
				<version>3.1.2</version>
			</plugin>
		</plugins>

		<resources>
			<resource>
				<directory>src/main/resources</directory>
				<excludes>
					<exclude>**/*.gitkeep</exclude>
				</excludes>
			</resource>

			<!-- We only allow properties files in the java source directories. All 
				other resource should go in the src/main/resources directory. -->
			<resource>
				<directory>src/main/java</directory>
				<includes>
					<include>**/*.properties</include>
				</includes>
			</resource>
		</resources>
	</build>

	<modules>
		<module>core/util</module>
		<module>core/croquet</module>
		<module>core/ide</module>
		<module>distribution</module>
	</modules>

	<repositories>
		<repository>
			<id>org.alice</id>
			<name>Alice Repository</name>
			<url>http://dev.lookingglass.wustl.edu/public/maven2/alice</url>
		</repository>
		<repository>
			<id>org.alice.external</id>
			<name>Alice External Repository</name>
			<url>http://dev.lookingglass.wustl.edu/public/maven2/external</url>
		</repository>
	</repositories>

	<!-- jogamp developer builds -->
	<profiles>
		<profile>
			<id>jogamp</id>
			<repositories>
				<repository>
					<id>jogamp-remote</id>
					<name>jogamp test mirror</name>
					<url>http://www.jogamp.org/deployment/maven/</url>
					<layout>default</layout>
				</repository>
			</repositories>
		</profile>
	</profiles>
	<!-- <activeProfiles> -->
	<!-- <activeProfile>jogamp</activeProfile> -->
	<!-- </activeProfiles> -->

</project>
